class Iron{
	constructor(x,y,)
	{
	// assign options to the rubber ball
		this.x=x;
		this.y=y;
		this.body=Bodies.rectMode(this.x, this.y, )
		World.add(world, this.body);

	}
	display()
	{
			var options = {
                'restitution':0.8,
                'friction':3,
                'density':30
                
            }
			rectMode(CENTER)
			strokeWeight(4);
			stroke("black");
			fill("darkblue");
			
			//draw the ellipse here to display the rubber ball

Pop()